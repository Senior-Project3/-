/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['media.istockphoto.com', 't3.ftcdn.net', 'images.unsplash.com', 'ps.w.org'],
    },
  };
  
  export default nextConfig;